{% extends 'base.html.twig' %}

{% block title %}New Plant Catalogue{% endblock %}

{% block body %}
    <h1>Create new Plant Catalogue</h1>
    {% dump member %}
    {{ include('plant_catalogue/_form.html.twig') }}

   
     <a href="{{ path('member_show', {'id': member.id}) }}", button class="btn btn-primary">Back to list</a>
{% endblock %}
