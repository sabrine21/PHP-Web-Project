{% extends 'base.html.twig' %}

{% block title %}Edit PlantCatalogue{% endblock %}

{% block body %}
    <h1>Edit PlantCatalogue</h1>

    {{ include('plant_catalogue/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="d-flex justify-content-between mt-3"> {# Utilisation de d-flex et justify-content-between #}
        <a href="{{ path('PlantCatalogue_index') }}" class="btn btn-primary">Back to list</a>
        {% if is_granted('ROLE_ADMIN') %}
            {{ include('plant_catalogue/_delete_form.html.twig') }}
        {% endif %}
    </div>
{% endblock %}
